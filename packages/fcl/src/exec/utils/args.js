/** 
 * @description Checks if a value is a Cadence composite type
 * @param {any} value - the value to check
 * @returns {boolean}
 */

// regex to parse cadence args

// array can only be number, bool, string
// array can be recursive

// maybe we oly support nubmer, string, bool