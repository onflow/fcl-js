/**
 * Asserts fact is true, otherwise throw an error with invariant message
 * @param fact
 * @param msg
 * @param rest
 */
export declare function invariant(fact: boolean, msg: string, ...rest: any[]): void;
